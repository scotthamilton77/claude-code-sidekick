{
  "name": "@sidekick/testing-fixtures",
  "version": "0.1.0",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write \"src/**/*.ts\""
  },
  "dependencies": {
    "@sidekick/types": "workspace:*",
    "yaml": "^2.8.2"
  },
  "peerDependencies": {
    "vitest": "^4.0.0"
  },
  "devDependencies": {
    "@sidekick/core": "workspace:*",
    "@types/node": "^22.7.4",
    "typescript": "^5.6.3",
    "vitest": "^4.0.18"
  }
}
