{
  "name": "@scotthamilton77/sidekick",
  "version": "0.0.2-alpha",
  "description": "AI pair programming assistant with personas, session tracking, and contextual nudges",
  "bin": {
    "sidekick": "dist/bin.js"
  },
  "files": [
    "dist",
    "assets"
  ],
  "scripts": {
    "clean": "rm -rf dist assets",
    "bundle:cli": "esbuild ../sidekick-cli/dist/bin.js --bundle --platform=node --format=cjs --main-fields=module,main --outfile=dist/bin.js --external:fsevents",
    "bundle:daemon": "esbuild ../sidekick-daemon/dist/index.js --bundle --platform=node --format=cjs --main-fields=module,main --outfile=dist/daemon.js --external:fsevents",
    "bundle:assets": "mkdir -p assets && cp -r ../sidekick-core/assets/sidekick assets/",
    "bundle": "pnpm run clean && pnpm run bundle:cli && pnpm run bundle:daemon && pnpm run bundle:assets",
    "prepublishOnly": "pnpm run bundle"
  },
  "keywords": [
    "claude",
    "claude-code",
    "ai",
    "assistant",
    "hooks",
    "personas"
  ],
  "author": "Scott Hamilton",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/scotthamilton77/claude-code-sidekick.git"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
