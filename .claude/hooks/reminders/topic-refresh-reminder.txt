**IMPORTANT:** This conversation's topic was previously set to:
```
${topic}
```

If it has changed, use Bash to run:
`${HOOK_DIR}/write-topic.sh '${session_id}' 'new concise topic (50 chars max)' 'snarky explanation of the pivot'`

If the conversation has devolved into unclear/vague territory, run:
`${HOOK_DIR}/write-unclear-topic.sh '${session_id}' 'cynical insult' instead.`
