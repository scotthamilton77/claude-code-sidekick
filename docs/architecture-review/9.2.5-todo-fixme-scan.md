# 9.2.5 TODO/FIXME/@deprecated Scan

## Summary
- Total TODOs: 0
- Total FIXMEs: 9
- Total @deprecated: 7
- Total HACK/XXX: 0

## Findings by Severity

### Blockers (must fix before release)

None identified.

### Important (should fix soon)

| File | Line | Type | Content |
|------|------|------|---------|
| `packages/sidekick-daemon/src/daemon.ts` | 624 | FIXME | P&R baseline update logic is inappropriately placed in daemon.ts instead of feature controller |
| `packages/sidekick-core/src/structured-logging.ts` | 383 | FIXME | Event routing logic is mixed with logging setup - inappropriate mixing of concerns |
| `packages/sidekick-core/src/structured-logging.ts` | 704 | FIXME | ReminderConsumed event defined in core instead of reminders feature package |
| `packages/sidekick-core/src/structured-logging.ts` | 884 | FIXME | ReminderStaged event defined in core instead of reminders feature package |
| `packages/sidekick-core/src/structured-logging.ts` | 919 | FIXME | SummaryUpdated event defined in core instead of summary feature package |
| `packages/sidekick-core/src/structured-logging.ts` | 962 | FIXME | SummarySkipped event defined in core instead of summary feature package |
| `packages/sidekick-core/src/structured-logging.ts` | 992 | FIXME | RemindersCleared event defined in core instead of reminders feature package |

### Nice-to-have (low priority)

| File | Line | Type | Content |
|------|------|------|---------|
| `packages/types/src/services/config.ts` | 15 | FIXME | Minimal config interfaces are a code smell suggesting architectural layering issue |
| `packages/sidekick-core/src/transcript-service.ts` | 1439 | FIXME | Old object format backward compat for currentContextTokens can be removed after migration period |
| `packages/feature-reminders/src/handlers/staging/stage-pause-and-reflect.ts` | 32 | FIXME | P&R baseline reset mechanism could be simplified via countdown or event handler pattern |
| `packages/feature-statusline/src/statusline-service.ts` | 635 | FIXME | Resume message display mode may be redundant with priority 1 logic |

## Findings by Package

### sidekick-core (6 issues)
- **Architectural concern**: Event definitions should be migrated to feature packages (5 FIXMEs in structured-logging.ts)
- **Mixing of concerns**: Logging file routing logic needs extraction (1 FIXME)

### sidekick-daemon (1 issue)
- **Handler placement**: P&R baseline update should move to feature controller

### feature-reminders (1 issue)
- **Implementation pattern**: Consider countdown-based reset mechanism

### feature-statusline (1 issue)
- **Display logic**: Resume message priority may be redundant

### types (1 issue)
- **Code smell**: Minimal interfaces need review

## @deprecated Items

| Item | Location | Migration Path | Assessment |
|------|----------|----------------|------------|
| `initialize()` method | `packages/types/src/services/transcript.ts:143` | Use `prepare()` + `start()` | Safe to remove after deps updated |
| `initialize()` method | `packages/sidekick-core/src/transcript-service.ts:233` | Use `prepare()` + `start()` | Safe to remove after deps updated |
| `initialize()` method | `packages/testing-fixtures/src/mocks/MockTranscriptService.ts:82` | Use `prepare()` + `start()` | Safe to remove after tests updated |
| `getTranscriptService()` method | `packages/types/src/services/service-factory.ts:33` | Use `prepareTranscriptService()` + `start()` | Safe to remove after implementation updated |
| `getTranscriptService()` method | `packages/sidekick-core/src/service-factory.ts:88` | Use `prepareTranscriptService()` + `start()` | Safe to remove after callers updated |
| `getSessionState()` method | `packages/feature-statusline/src/state-reader.ts:118` | Use `getTranscriptMetrics()` instead | Safe to remove after callers updated |
| `SessionMetricsState` / `SessionMetricsStateSchema` | `packages/types/src/services/state.ts:161-164` | Use `TranscriptMetricsState` instead | Safe to remove after all imports updated |
| `EMPTY_SESSION_STATE` constant | `packages/feature-statusline/src/types.ts:270` | Use `EMPTY_TRANSCRIPT_STATE` instead | Safe to remove after all imports updated |

## Relevance to Later Phases

### Phase 9.5 (Feature Domain Consolidation)
- 5 FIXMEs in structured-logging.ts indicate event definitions need to move to feature packages
- daemon.ts P&R baseline logic should move to feature-reminders

### Phase 9.7 (Code Documentation Polish)
- All deprecated items can be cleaned up after migration
- Backward compat code in transcript-service.ts can be removed
