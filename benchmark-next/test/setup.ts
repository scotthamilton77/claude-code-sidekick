/**
 * Vitest global setup
 * Runs before all tests
 */

import { beforeAll, afterAll } from 'vitest'

beforeAll(() => {
  // Global test setup
  // e.g., set environment variables, initialize mocks
})

afterAll(() => {
  // Global test teardown
  // e.g., cleanup temp files, close connections
})
