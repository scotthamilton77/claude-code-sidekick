# Testing Fixtures Package

## Scope

Placeholder for documenting the shared testing/fixtures workspace package. Establishes how mock transcripts, CLI harnesses, and golden files are structured for Node/Vitest suites.

## To Be Defined

- Directory layout and naming conventions for fixtures referenced by packages.
- APIs/helpers exported for CLI/supervisor integration tests.
- Strategy for syncing fixtures with Bash-era test data (e.g., `test-data/` reuse).
- Guidance on keeping fixtures deterministic and small enough for repo inclusion.
